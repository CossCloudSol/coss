/**
 * /admin/topbar
 *
 * Canonical URL for the Topbar / Announcement Bar admin page.
 * Re-exports the same component as /admin/announcement-bar so both
 * URLs work — the sidebar links here; the old URL still functions.
 */
export { default } from '../announcement-bar/page';
